[workspace]
members = [
  # the main library and tests
  "rustls",
  # tests and example code that depend on mio
  "rustls-mio",
]
exclude = ["admin/rustfmt"]
resolver = "2"

[patch.crates-io]
webpki = { git = "https://github.com/ctz/webpki", branch = "feat-ip-address" }
